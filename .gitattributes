# Force LF for shell scripts, PowerShell scripts and systemd units, so
# deploy.ps1 and the service file work on Windows checkouts
# (core.autocrlf=true would otherwise convert to CRLF and break bash).
*.sh         text eol=lf
*.ps1        text eol=lf
*.service    text eol=lf
